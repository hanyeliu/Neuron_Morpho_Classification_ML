# Xavier Vasques
# Laboratoire de Recherche en Neurosciences Cliniques (LRENC)
# Update May, 1st 2016

import time
import configuration


def lvltrace(endroit) :
       pass
      # fin=0
      # debut=0
      # print("************************************************************")
      # if configuration.count == 0 :
      #    print (str(time.strftime("%H:%M:%S"))+":"+endroit)
      #    configuration.debut=int(round(time.time() * 1000))
      #    configuration.count =1
      # else :
      #    fin = int(round(time.time() * 1000))
      #    print (str(time.strftime("%H:%M:%S"))+": "+endroit+" temps d'execution est " +str(fin-configuration.debut)+" ms")
      #    configuration.count = 0

      # print("************************************************************")
